# Runtime dependencies for the Sing Khmer mapping engine.
# None yet — the Phase 1 prototype targets the Python standard library.
# Add runtime packages here as the engine grows (e.g. a fuzzy-matching lib for step 1.8).

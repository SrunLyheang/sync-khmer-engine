# Development dependencies (testing, local server, spreadsheet export).
# Install with: pip install -r requirements-dev.txt
-r requirements.txt

pytest>=8.0
uvicorn[standard]>=0.30
httpx>=0.27
openpyxl>=3.1
